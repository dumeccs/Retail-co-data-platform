# ── ERP API ───────────────────────────────────────────────────────────────────
ERP_API_KEY=your_api_key_here
ERP_BASE_URL=https://hngstage8da-55c7f5f769c8.herokuapp.com

# ── Lake database (Postgres 15) ───────────────────────────────────────────────
LAKE_POSTGRES_HOST=lake-postgres
LAKE_POSTGRES_PORT=5432
LAKE_POSTGRES_DB=lake_db
LAKE_POSTGRES_USER=lake_user
LAKE_POSTGRES_PASSWORD=change_me

# ── Warehouse database (Postgres 15) ─────────────────────────────────────────
WAREHOUSE_POSTGRES_HOST=warehouse-postgres
WAREHOUSE_POSTGRES_PORT=5432
WAREHOUSE_POSTGRES_DB=warehouse_db
WAREHOUSE_POSTGRES_USER=warehouse_user
WAREHOUSE_POSTGRES_PASSWORD=change_me

# ── Airflow metadata database ─────────────────────────────────────────────────
AIRFLOW_POSTGRES_USER=airflow
AIRFLOW_POSTGRES_PASSWORD=change_me
AIRFLOW_POSTGRES_DB=airflow

# ── Airflow core ──────────────────────────────────────────────────────────────
# Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW__CORE__FERNET_KEY=your_fernet_key_here
AIRFLOW_UID=50000
